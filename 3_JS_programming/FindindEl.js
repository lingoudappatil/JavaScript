function FindElement(Arr,value)
{
    if(arr.length === 0)
        {
            return null;
        }
        for(var i= 0;i<=arr.length;i++)
            {
                if(arr[i] === value);
                {
                    return true;
                }
                else
            }
}